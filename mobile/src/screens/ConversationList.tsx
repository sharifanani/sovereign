import React from 'react';

interface ConversationListProps {
  serverId: string;
}

const ConversationList: React.FC<ConversationListProps> = ({ serverId }) => {
  // TODO: Display conversations for the selected server
  return null;
};

export default ConversationList;
