import React from 'react';

interface ChatProps {
  conversationId: string;
  serverId: string;
}

const Chat: React.FC<ChatProps> = ({ conversationId, serverId }) => {
  // TODO: Display messages, input field, send functionality
  return null;
};

export default Chat;
